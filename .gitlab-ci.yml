image: node:18

stages:
  - build
  - deploy

variables:
  NODE_ENV: development
  RAILWAY_TOKEN: $RAILWAY_TOKEN  # Set this in GitLab CI/CD variables



deployBackend:
  stage: deploy
  script:
    - cd backend/
    - npm install
    - npm exec railway --version
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      changes: 
        - backend/**/*

# deployBackend:
#   stage: deploy
#   script:
#     - npm exec railway up # Deploy using Railway CLI
#   rules:
#     - if: $CI_COMMIT_BRANCH == "master"
#       changes: 
#         - backend/**/*
